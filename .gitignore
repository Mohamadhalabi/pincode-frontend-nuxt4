# Nuxt dev/build output
.nuxt
.output
dist

# Node dependencies
node_modules
yarn.lock
package-lock.json
# (Keep package-lock.json if you want to lock versions, otherwise ignore it. Usually, it is kept.)

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (Critical for security)
.env
.env.production
.env.development
.env.local

# System files
.DS_Store
Thumbs.db
.idea
.vscode